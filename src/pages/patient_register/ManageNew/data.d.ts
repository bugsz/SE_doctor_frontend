export type patientItem = {
  // patient_id: string
  // patient_name: string;
  // register_time: string;
  user_id: string;
  name: string;
  time: string;
};

export type registerReqType = {
  date_curr: string;
  doctor_id: string;
}